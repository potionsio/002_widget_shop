# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# The demo inventory lives in `WidgetShop.Demo` so it can also be
# re-seeded on a deployed release, where Mix isn't available:
#
#     bin/widget_shop rpc "WidgetShop.Demo.reset!()"

WidgetShop.Demo.reset!()
